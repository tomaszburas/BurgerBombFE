import styled from 'styled-components';

export const NoData = () => {
    return <Container>No Data</Container>;
};

const Container = styled.div`
    width: 100%;
    text-align: center;
    margin-top: 1rem;
    font-style: italic;
`;
